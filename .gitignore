# Secrets — never commit these
.env*
!.env.example

# Local files
.DS_STORE
models/files/.cache
models/model_cards/*
models/*.md
models/default.yaml

# Benchmark outputs (JSONs in subfolders, md results tracked)
models/benchmarks/**/*.json
